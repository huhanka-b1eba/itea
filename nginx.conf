events {
    worker_connections 1024;
}

http {
    upstream itea_app {
        server itea-app:3001;
    }

    server {
        listen 80;
        server_name iteadev.ru;

        # Редирект на HTTPS (раскомментируйте после настройки SSL)
        # return 301 https://$server_name$request_uri;

        location / {
            proxy_pass http://itea_app;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # HTTPS конфигурация (раскомментируйте после настройки SSL)
    # server {
    #     listen 443 ssl http2;
    #     server_name 185.139.69.170;
    #
    #     ssl_certificate /etc/nginx/ssl/cert.pem;
    #     ssl_certificate_key /etc/nginx/ssl/key.pem;
    #
    #     location / {
    #         proxy_pass http://itea_app;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
